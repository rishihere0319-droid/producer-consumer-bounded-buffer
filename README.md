# producer-consumer-bounded-buffer
A thread-safe Python implementation of the classic Producer-Consumer problem using a bounded circular buffer. It demonstrates concurrent process synchronization and race-condition prevention using semaphores for slot availability and a mutex lock for critical section access.
